version: "3.9"

services:
  cloud-monitoring-system:
    build:
      context: ..
      dockerfile: docker/Dockerfile

    container_name: cloud-monitoring-system

    ports:
      - "5000:5000"

    restart: unless-stopped
